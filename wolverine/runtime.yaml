name: wolverine-tracker
version: 3.0.2
description: >
  Wolverine v3.0.2 — disable hard_timeout (complete time-cuts removal
  on single-asset HYPE). Pairs with v3.0.1's dead_weight_cut disable.
  v1 DSL fires hard_timeout in Phase 2 incorrectly (per v2 spec it's
  Phase-1-only) — observed on Polar v3.0.4 overnight 2026-04-23.
  Wolverine shares the same v1 DSL bug. v3.0 had hard_timeout at 6h
  which was particularly aggressive for HYPE — this fix matters most
  here. Phase 1 (20% max_loss) + Phase 2 tiers (HYPE-tight ladder)
  own all exits. Scanner unchanged (Kodiak-family gates preserved).

strategy:
  wallet: "${WALLET_ADDRESS}"
  budget: 1000
  slots: 1
  margin_per_slot: 250
  enabled: true

scanners:
  - name: position_tracker
    type: position_tracker
    interval: 10s

actions:
  - name: position_tracker_action
    action_type: POSITION_TRACKER
    decision_mode: rule
    scanners: [position_tracker]

# ─── EXIT (DSL) — HYPE-tuned, tighter than Kodiak SOL ──────────
# HYPE reverses faster than SOL. Triggers and locks compressed to catch
# profit earlier. Hard_timeout 6h — HYPE moves are typically hours, not
# days (unlike BTC which needs 24h).
#
# Phase 2 matches scanner's _DSL_TIERS_DEFAULT via config.json dslTiers
# (scanner attaches via ratchet_stop_add; runtime.yaml is operator
# reference and for backup DSL engine behavior).
exit:
  engine: dsl
  interval_seconds: 30
  dsl_preset:
    hard_timeout:
      enabled: false                 # v3.0.2: DISABLED. v1 DSL fires this in Phase 2 incorrectly. Phase 1 + Phase 2 own all exits.
      interval_in_minutes: 360
    weak_peak_cut:
      enabled: true
      interval_in_minutes: 90
      min_value: 3.0
    dead_weight_cut:
      enabled: false                 # v3.0.1: DISABLED. Single-asset agent has no rotation opportunity cost.
      interval_in_minutes: 45
    phase1:
      enabled: true
      max_loss_pct: 20.0
      retrace_threshold: 8
      consecutive_breaches_required: 3
    phase2:
      enabled: true
      tiers:
        - { trigger_pct: 10, lock_hw_pct: 15 }
        - { trigger_pct: 20, lock_hw_pct: 35 }
        - { trigger_pct: 35, lock_hw_pct: 55 }
        - { trigger_pct: 55, lock_hw_pct: 70 }
        - { trigger_pct: 80, lock_hw_pct: 85 }

notifications:
  telegram_chat_id: "${TELEGRAM_CHAT_ID}"
