{
  "_comment": "JAGUAR v4.0.0 config — simplified for senpi_runtime_helpers / v2-runtime-native architecture. The runtime owns position tracking, daily caps (max_entries_per_day=3 with bypass-on-profit), per-asset cooldowns, drawdown gates, and consecutive-loss halts via runtime.yaml's risk.guard_rails block. The producer reads only the fields below.",
  "strategyId": "",
  "wallet": "",
  "chatId": ""
}
