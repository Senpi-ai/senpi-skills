{
  "_comment": "VULTURE v3.0.0 config — simplified for v2-runtime-native architecture. The runtime owns position tracking, daily caps, cooldowns, drawdown gates, and consecutive-loss halts via runtime.yaml's risk.guard_rails block. The producer reads only the few fields below.",
  "strategyId": "",
  "wallet": "",
  "chatId": "",
  "quietHours": {
    "_comment": "FP-001: skip emission during low-liquidity UTC window. Apex setups (best signal score >= apexBypassScore) bypass. Set startUtc == endUtc to disable.",
    "startUtc": 0,
    "endUtc": 4,
    "apexBypassScore": 11
  }
}
